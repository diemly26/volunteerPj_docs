# assumptions.md

- Owner user MUST come from module 01 users table.
- Required document type list MUST be configured in module 12 `system_config`.
- Trusted toggle MUST be admin-only action.
- Document files MUST already be stored and represented by `file_url`.
