# assumptions.md

- Access token TTL MUST be configured in `system_config` module 12.
- Refresh token TTL MUST be configured in `system_config` module 12.
- OTP purpose `REGISTER_VERIFY` MUST be default for registration flow.
- Password reset flow is out of scope for module 01 documents.
